# ══════════════════════════════════════════════════════════════════════
#  Nasal Planner — зависимости САМОГО ПРИЛОЖЕНИЯ
# ══════════════════════════════════════════════════════════════════════
#
#  Окружение, из которого собирается .exe (.venv-build) и запускается
#  entry.py при разработке.
#
#  ЗДЕСЬ НЕТ torch, monai, SimpleITK, pymeshlab — они живут во ВТОРОМ
#  окружении и вызываются как внешний процесс
#  (см. requirements-inference.txt). Приложение весит ~250 МБ,
#  torch с зависимостями — ещё 2-4 ГБ.
#
#  УСТАНОВКА
#      python -m venv .venv-build
#      .\.venv-build\Scripts\activate
#      pip install -r requirements.txt
#

# ══════════════════════════════════════════════════════════════════════

# ── Веб-сервер ────────────────────────────────────────────────────────
flask==3.1.0

pywebview==6.2.1
pythonnet ; sys_platform == "win32"
trimesh==4.10.1
numpy==2.2.6
scipy==1.15.3
networkx

rtree
embreex

pymeshfix

pynrrd

pyinstaller

