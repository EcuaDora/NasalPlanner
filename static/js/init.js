/* ─── init ─────────────────────────────────────────────────────
   Последний скрипт. Ничего интерактивного на этапе 0 не делает,
   но логирует старт — видно в консоли браузера (F12).
──────────────────────────────────────────────────────────────── */
document.addEventListener('DOMContentLoaded', function () {
  console.log('[Nasal Planner] Запущено. Модулей в реестре:',
              window.Modules ? window.Modules.all().length : 0);
});
